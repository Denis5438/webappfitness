# 🚀 Инструкция по обновлению деплоя (V2)

Поскольку мы обновили frontend (новая папка `frontend-new`) и добавили новые функции, следуйте этому гайду.

## 1. Backend (Серверная часть)

Если вы используете **Railway** или **Render**:

1.  **Environment Variables (Переменные окружения)**:
    Убедитесь, что добавлены эти переменные (в Dashboard вашего хостинга):
    *   `MONGODB_URI`: (Обязательно) Ссылка на вашу базу MongoDB (например, от MongoDB Atlas).
    *   `TELEGRAM_BOT_TOKEN`: Ваш токен бота.
    *   `CORS_ORIGINS`: Добавьте url вашего нового фронтенда (если изменится).

2.  **Обновление кода**:
    *   Если у вас настроен авто-деплой с GitHub: просто сделайте `git push`.
    *   Если вручную: загрузите содержимое папки `backend` на сервер.

## 2. Frontend (Клиентская часть)

**Важно:** Теперь мы используем папку `frontend-new`, а не `frontend`.

### Деплой на Netlify (Рекомендуется)

1.  Зайдите на [Netlify](https://app.netlify.com).
2.  Выберите ваш проект -> **Site configuration** -> **Build & deploy**.
3.  Измените настройки сборки:
    *   **Base directory**: `frontend-new` (Это важно! Раньше было `frontend`)
    *   **Build command**: `npm run build`
    *   **Publish directory**: `frontend-new/dist`
4.  Зайдите в **Environment variables**:
    *   Убедитесь, что `VITE_API_URL` указывает на ваш *живой* backend (например, `https://ваше-приложение.up.railway.app/api`).
5.  Нажмите **Trigger deploy** (Deploy site).

### Если вы деплоите вручную (Drag & Drop)

1.  Откройте терминал в папке проекта.
2.  Перейдите в новую папку и соберите проект:
    ```bash
    cd frontend-new
    npm install
    npm run build
    ```
3.  В папке `frontend-new` появится папка `dist`.
4.  Перетащите папку `dist` в окно деплоя Netlify.

## ✅ Чек-лист проверки
*   [ ] Backend отвечает на запросы (проверьте `/api` или `/`).
*   [ ] Frontend открывается.
*   [ ] Вкладка "Лента" загружается (если нет поста, создайте первый).
*   [ ] Лайки/Комментарии работают.
