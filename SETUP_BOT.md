# 🤖 Настройка Telegram бота

## Шаг 1: Создание бота через BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `Fitness Diary`)
4. Введите username бота (например: `fitness_diary_bot`)
5. **Сохраните токен бота** — он понадобится для `.env`

Пример токена: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

## Шаг 2: Настройка Web App

1. В BotFather отправьте `/mybots`
2. Выберите вашего бота
3. Нажмите `Bot Settings` → `Menu Button`
4. Выберите `Edit Menu Button URL`
5. Введите URL вашего frontend:
   - **Для разработки**: `http://localhost:5173` (не будет работать в Telegram, только для теста)
   - **Для продакшена**: `https://your-domain.com`

## Шаг 3: Настройка .env файла

```bash
cd backend
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Telegram
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
WEBAPP_URL=https://your-domain.com  # или http://localhost:5173 для теста
```

## Шаг 4: Установка зависимостей

```bash
npm install telegraf concurrently
```

## Шаг 5: Запуск бота

### Вариант 1: Запустить только бота
```bash
npm run bot
```

### Вариант 2: Запустить бота + API сервер одновременно
```bash
npm run dev:all
```

### Вариант 3: Запустить отдельно (в разных терминалах)
```bash
# Терминал 1 - API сервер
npm run dev

# Терминал 2 - Telegram бот
npm run bot
```

## Шаг 6: Тестирование

1. Найдите вашего бота в Telegram по username
2. Отправьте `/start`
3. Вы должны увидеть приветственное сообщение с кнопкой **"🏋️ Открыть приложение"**
4. Нажмите на кнопку — откроется WebApp

## Команды бота

- `/start` - Приветствие и кнопка для открытия WebApp
- `/help` - Справка
- `/stats` - Быстрая статистика

## Деплой на продакшен

### Для WebApp (Frontend)

Вы можете задеплоить на:
- **Vercel** (бесплатно): `vercel deploy`
- **Netlify** (бесплатно): `netlify deploy`
- **GitHub Pages** (бесплатно)

### Для Backend + Bot

Вы можете задеплоить на:
- **Railway** (бесплатно до 500 часов/месяц)
- **Render** (бесплатно с ограничениями)
- **VPS** (DigitalOcean, Hetzner и т.д.)

### Пример деплоя на Railway

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Добавьте переменные окружения из `.env`
5. Railway автоматически запустит `npm start`

Для запуска бота на Railway добавьте в `package.json`:

```json
"scripts": {
  "start": "node src/index.js & node src/bot.js"
}
```

## Troubleshooting

### Бот не отвечает
- Проверьте, что токен бота правильный в `.env`
- Убедитесь, что бот запущен (`npm run bot`)
- Проверьте логи в консоли

### WebApp не открывается
- Убедитесь, что `WEBAPP_URL` правильный
- Для локальной разработки используйте ngrok: `ngrok http 5173`
- В продакшене используйте HTTPS URL

### Ошибка "Cannot find module 'telegraf'"
```bash
npm install telegraf
```

## Дополнительные возможности

Вы можете расширить бота:

1. **Уведомления о тренировках**
   - Напоминания через определенное время
   - Мотивационные сообщения

2. **Быстрые команды**
   - `/today` - Тренировка на сегодня
   - `/progress` - График прогресса

3. **Inline режим**
   - Поделиться тренировкой с друзьями
   - Поиск упражнений

4. **Платежи через Telegram Stars**
   - Покупка Pro подписки
   - Покупка программ тренировок
