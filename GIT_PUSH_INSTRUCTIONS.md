# 🐙 Как загрузить изменения на GitHub

Поскольку вы добавили новые функции и папку `frontend-new`, нужно обновить ваш репозиторий.

## Проверка статуса

Откройте терминал в папке `webapp` (корневой папке проекта) и введите:

```bash
git status
```

Если вы видите ошибку "not a git repository", значит нужно инициализировать репозиторий.

## Вариант 1: У вас уже есть репозиторий (но он не подключен)

1.  **Инициализация (если еще не делали во всей папке):**
    ```bash
    git init
    ```

2.  **Добавление всех файлов:**
    ```bash
    git add .
    ```
    *(Точка означает "все файлы")*

3.  **Сохранение изменений (коммит):**
    ```bash
    git commit -m "Добавлена Социальная Лента и обновлен UI"
    ```

4.  **Подключение к GitHub (если не подключено):**
    *   Создайте новый репозиторий на [GitHub.com](https://github.com/new).
    *   Скопируйте ссылку (https://github.com/ВАШ_НИК/ВАШ_РЕПО.git).
    *   Введите команду:
    ```bash
    git remote add origin https://github.com/Denis5438/webappfitness.git
    ```
    *(Если скажет "remote origin already exists", пропустите этот шаг)*

5.  **Отправка на сервер (Push):**
    ```bash
    git push -u origin main
    ```
    *(Или `master`, если ветка называется master)*

## Вариант 2: Если вы просто хотите обновить существующий

Если `git status` показывает список красных файлов:

```bash
git add .
git commit -m "Update frontend and add social feed"
git push
```

## 📱 Исправление меню на телефоне

Пока вы спрашивали, я также **поправил нижнее меню**:
*   Теперь оно **скроллится вбок**, если иконок слишком много.
*   Кнопки не сжимаются и их удобно нажимать.
*   "Модер" и другие иконки теперь всегда доступны свайпом.

Попробуйте открыть на телефоне после обновления!
